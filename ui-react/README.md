# React UI (E-FlipkartLite)

## Run

```bash
cd ui-react
npm install
npm run dev
```

Open: `http://localhost:5173`

## Notes
- Vite proxy is configured to route `/auth`, `/customer`, `/agent` to gateway `http://localhost:8080`.
- Start backend services first.
- Login with customer or agent to see role-based dashboard.
