server:
  port: 8084

spring:
  application:
    name: cart-service
  config:
    import: optional:file:../application-db.yml

management:
  endpoints:
    web:
      exposure:
        include: health,info
