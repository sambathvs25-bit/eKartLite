server:
  port: 8082

spring:
  application:
    name: customer-service
  config:
    import: optional:file:../application-db.yml

management:
  endpoints:
    web:
      exposure:
        include: health,info
